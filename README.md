# kennisbank-beheer-scripts
Jupyter Notebook scripts die gebruikt worden om in bulk updates op de kennisbank door ter voeren. 

## Installatie

### Git Bash
```
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
```

## How to Run
```
python -m notebook batch_add_pages.ipynb
```
